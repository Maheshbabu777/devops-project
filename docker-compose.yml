version: '3.8'
services:
  web:
    build: .
    ports:
      - "5000:5000"
    restart: always
    volumes:
      - db-data:/app/data
    environment:
      - DATABASE=/app/data/todos.db

volumes:
  db-data: